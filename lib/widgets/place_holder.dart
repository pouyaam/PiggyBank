import 'package:flutter/material.dart';

class PlaceHolder extends StatelessWidget {
  final Widget widget;

  const PlaceHolder(this.widget, {Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
